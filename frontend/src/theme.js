import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#8d1f26",
    },
    background: {
      default: "#f6f1ea",
      paper: "#ffffff",
    },
  },
});

export default theme;
